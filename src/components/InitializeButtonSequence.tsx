"use client";

import { useEffect } from 'react';
// import { initializeButtonSequenceDetection } from "@/services/buttonSequenceService";

export function InitializeButtonSequence() {
  useEffect(() => {
    // initializeButtonSequenceDetection();
  }, []);

  return null; // This component doesn't render anything
}

